---
// BentoGrid Component
// "Pourquoi travailler avec moi" section with bento grid layout
---

<section class="value reveal" id="why-me">
    <div class="section-header">
        <div class="section-label">Ma Différence</div>
        <h2 class="section-title">Pourquoi travailler avec moi</h2>
    </div>
    
    <div class="value-grid">
        <article class="value-card">
            <span class="value-icon">🎯</span>
            <h3>Vision Business & Technique</h3>
            <p>Je parle les deux langues : celle des développeurs ET celle des décideurs. Je ne me contente pas de configurer des tags — je traduis vos données en recommandations stratégiques qui impactent directement votre bottom line.</p>
        </article>
        
        <article class="value-card">
            <span class="value-icon">🔍</span>
            <h3>Expertise Premium</h3>
            <p>Expérience avec des clients majeurs (Proximus, Mazda, Polestar). Maîtrise complète de l'écosystème Google et des outils d'analyse avancés.</p>
        </article>
        
        <article class="value-card">
            <span class="value-icon">📊</span>
            <h3>Data-Driven</h3>
            <p>Framework AARRR, analyse de funnel, CRO et expérimentation continue.</p>
        </article>
        
        <article class="value-card">
            <span class="value-icon">⚡</span>
            <h3>Résultats Mesurables</h3>
            <p>Tracking solide → données fiables → insights actionnables → ROI mesurable. Chaque recommandation est ancrée dans la data.</p>
        </article>
        
        <article class="value-card">
            <span class="value-icon">🚀</span>
            <h3>Réactif</h3>
            <p>Freelance indépendant, disponible et agile. Pas de bureaucratie, des résultats.</p>
        </article>
    </div>
</section>

<style>
    .value {
        padding: 8rem 4rem;
        max-width: 1400px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
        background: rgba(10, 10, 10, 0.3);
    }

    .section-header {
        text-align: center;
        margin-bottom: 4rem;
    }

    .section-label {
        font-size: 0.85rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        background: linear-gradient(135deg, var(--color-gold) 0%, var(--color-blue) 100%);
        background-size: 200% 200%;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 1rem;
        animation: gradientShift 3s ease infinite;
    }

    .section-title {
        font-family: var(--font-display);
        font-size: clamp(2.5rem, 5vw, 4.5rem);
        font-weight: 800;
        line-height: 1.2;
        letter-spacing: -0.02em;
    }

    .value-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1.5rem;
    }

    .value-card {
        padding: 2.5rem;
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(20px);
        border: 1px solid var(--color-border);
        border-radius: 20px;
        transition: all 0.4s ease;
        position: relative;
        overflow: hidden;
    }

    /* Bento Grid Layout */
    .value-card:nth-child(1) {
        grid-column: 1 / 3;
        grid-row: 1 / 2;
    }

    .value-card:nth-child(2) {
        grid-column: 3 / 5;
        grid-row: 1 / 2;
    }

    .value-card:nth-child(3) {
        grid-column: 1 / 2;
        grid-row: 2 / 3;
    }

    .value-card:nth-child(4) {
        grid-column: 2 / 4;
        grid-row: 2 / 3;
    }

    .value-card:nth-child(5) {
        grid-column: 4 / 5;
        grid-row: 2 / 3;
    }

    .value-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 3px;
        background: linear-gradient(90deg, var(--color-gold) 0%, var(--color-blue) 100%);
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.4s ease;
    }

    .value-card:hover {
        background: rgba(255, 255, 255, 0.06);
        transform: translateY(-8px);
        border-color: var(--color-blue);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }

    .value-card:hover::before {
        transform: scaleX(1);
    }

    .value-icon {
        font-size: 2.5rem;
        margin-bottom: 1.25rem;
        display: block;
    }

    .value-card h3 {
        font-family: var(--font-display);
        font-size: 1.25rem;
        font-weight: 700;
        margin-bottom: 0.75rem;
        color: var(--color-blue);
    }

    .value-card p {
        color: var(--color-gray);
        font-size: 0.9rem;
        line-height: 1.7;
    }

    /* Responsive */
    @media (max-width: 1024px) {
        .value {
            padding: 5rem 2rem;
        }

        .value-grid {
            grid-template-columns: 1fr;
        }

        .value-card:nth-child(1),
        .value-card:nth-child(2),
        .value-card:nth-child(3),
        .value-card:nth-child(4),
        .value-card:nth-child(5) {
            grid-column: 1 / -1;
            grid-row: auto;
        }
    }
</style>
